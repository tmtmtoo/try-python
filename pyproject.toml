[project]
name = "try-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pyright>=1.1.391",
    "pytest>=8.3.5",
    "ruff>=0.8.4",
]

[tool.uv.workspace]
members = ["packages/*"]
